get:
  summary: Retrieve a group
  description: >-
    Retrieves the details of a given group

    #### Permissions

    Any user can retrieve a group.

    Authentication is not required to view the details of a specific group.

    #### Returns

    Returns a JSON object with a data key that has a representation of the serialized group.

  parameters:
    - in: path
      type: string
      name: group_id
      required: true
      description: 'The unique identifier of the group.'
  tags:
    - Groups
  operationId: groups_read
  x-response-schema: Group
  responses:
    '200':
      description: 'OK'
      schema:
        $ref: 'definition.yaml'
      examples:
        application/json:
          data:
          - relationships:
              members:
                links:
                  related:
                    href: https://api.osf.io/v2/groups/5c0571f4b146c900085f39a3/members/
                    meta: {}
            links:
              self: https://api.ßosf.io/v2/groups/5c0571f4b146c900085f39a3/
              html: https://osf.io/5c0571f4b146c900085f39a3/
            attributes:
              date_modified: '2017-02-09T20:37:49.060000'
              name: 'COS Science Lab'
              date_created: '2013-10-22T20:07:57.674000'
            type: groups
            id: 5c0571f4b146c900085f39a3
          links:
            self: https://api.osf.io/v2/groups/
            first:
            last:
            prev:
            next:
            meta:
              total: 1
              per_page: 10

patch:
  summary: Update a group
  description: >-
    Updates a group by setting the values of the attributes specified in the request body.
    Any unspecified attributes will be left unchanged.


    Groups can be updated with either a **PUT** or **PATCH** request.
    The `name` field is mandatory in a **PUT** request, and optional in a **PATCH**.


    #### Permissions

    Only managers of a group may update the group.
    Attempting to update a group for which you do not have manager access will result in a **403 Forbidden** response.


    #### Returns


    Returns a JSON object with a `data` key containing the new representation of the updated
    node, if the request is successful.


    If the request is unsuccessful, an `errors` key containing information about the failure will be returned.
    Refer to the [list of error codes](#tag/Errors-and-Error-Codes) to understand why this request may have failed.


  parameters:
    - in: path
      type: string
      name: group_id
      required: true
      description: 'The unique identifier of the group.'
    - in: body
      name: body
      required: true
      schema:
        type: object
        example:
          data:
            type: 'groups'
            id: '{group_id}'
            attributes:
              name: 'My new group name'

  tags:
    - Groups
  operationId: group_partial_update
  x-response-schema: Group
  consumes:
    - application/json
  responses:
    '200':
      description: 'OK'

delete:
  summary: Delete a group
  description: >-
    Permanently deletes a group. This action cannot be undone.


    #### Permissions


    Only group managers may delete a group.
    Attempting to delete a group for which you are not a manager will result in a **403 Forbidden** response.


    #### Returns


    If the request is successful, no content is returned.


    If the request is unsuccessful, a JSON object with an `errors` key containing information about the failure will be returned.
    Refer to the [list of error codes](#tag/Errors-and-Error-Codes) to understand why this request may have failed.
  parameters:
    - in: path
      type: string
      name: group_id
      required: true
      description: 'The unique identifier of the group.'
  tags:
    - Groups
  operationId: groups_delete
  responses:
    '204':
      description: 'No content'
